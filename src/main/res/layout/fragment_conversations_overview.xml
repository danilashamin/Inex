<!--android.support.v4.widget.SlidingPaneLayout xmlns:android="http://schemas.android.com/apk/res/android" -->
<ru.messenger.inex_messenger.SlidingPanel.SlidingPaneLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/content_view_spl"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="?attr/color_background_primary"
        android:orientation="vertical"
        android:weightSum="1">

        <de.timroes.android.listview.EnhancedListView
            android:id="@+id/list"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="0"
            android:background="?attr/color_background_primary"
            android:divider="@color/black12"
            android:dividerHeight="1dp" />
    </LinearLayout>

    <LinearLayout
        android:id="@+id/selected_conversation"
        android:layout_width="fill_parent"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:orientation="vertical" />

</ru.messenger.inex_messenger.SlidingPanel.SlidingPaneLayout>
